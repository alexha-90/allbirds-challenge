const MOCK_SUBMIT_URL = 'https://jsonplaceholder.typicode.com/todos/1'


export async function submitLoginData(data: any) {
    try {
        console.log('data', data )
        // const res = await fetch(url, {
        //     method: "GET",
        //     ...headers,
        // });

        // return await timeoutUtil(endpoint, headers, ms);
    } catch (err) {
        return null;
    }
}
